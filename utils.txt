Stack:
- Python 3.10+ (recommandé 3.11)
- FastAPI
- Uvicorn
- SQLModel
- SQLite

Installation:
python -m venv .venv
.venv\Scripts\activate

python.exe -m pip install --upgrade pip
pip install fastapi uvicorn sqlmodel
pip install "pydantic-ai-slim[openai]"
pip install -U langgraph
pip install "pydantic-ai-slim[mcp]"


Lancement: python main.py / uvicorn app.api.app:app --reload --port 8000
python scripts/mcp_chat.py
npx -y @modelcontextprotocol/inspector

Swagger : http://localhost:8000/docs

Ollama:
ollama --version
ollama pull llama3.1:8b
test: curl http://localhost:11434/v1/models

Ex Prompt:
- Suggestion: Triage le ticket 2 et explique brièvement pourquoi.
- Apply: Applique le triage sur le ticket 2.
- Batch: Applique le triage sur les tickets 1 à 5, et résume les changements.